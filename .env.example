# Database (set automatically by Railway)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Frontend API URL (for Vite, only needed if API is on a different domain in production)
# VITE_API_URL=https://your-railway-url.up.railway.app/api

# Server port (set automatically by Railway)
# PORT=3001

# Node environment
# NODE_ENV=production
